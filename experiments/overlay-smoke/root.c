/* Independent overlay-link experiment, not reconstructed game source. */
int one(), two();
main()
{
    if (one(7) != 8) return 11;
    if (two(9) != 18) return 12;
    if (one(3) != 4) return 13;
    return 0;
}
