# Historical overlay container ov14

Logical hunk 14; its physical boundary is observed in the executable.
The directory name is reconstructed from the hunk number, not an original
source filename. No reconstructed C/ASM or matching-source proof yet.
See `docs/modules.json` and `evidence/executable/overlay-tree.json`.
